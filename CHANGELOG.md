# Changelog

本项目遵循语义化版本号的思路进行版本归档。当前仓库从已在绿联 NAS 实机验证的 `v0.1.3` 开始归档。

## v0.1.4 - 2026-08-17

### 集合维护

- 新增“已整理集合”入口，自动发现带 `tvshow.nfo` 或绿联兼容 `Season 01 + SxxExx` 结构的集合。
- 集合详情页可查看集合信息、Episode 列表、海报/背景存在状态与完整性检查结果。
- 支持修改集合标题、简介、年份、Genre；重写 `tvshow.nfo` 时同步已有 Episode NFO 的 `showtitle`。
- 支持修改单集标题、简介和日期；只更新对应 NFO，不主动重命名视频文件。
- 集合级和单集级 NFO 更新均写入任务记录，可撤销恢复修改前内容。

### 增量追加

- 可向已有集合继续添加新视频。
- 自动读取 Season 01 当前最大 Episode 编号，从下一集连续编号。
- 追加前可编辑新增单集标题、简介和日期。
- 追加前提供文件移动与媒资生成预览及冲突检测。
- 只处理新增视频，不修改已有 Episode。
- 增量任务支持撤销，视频可恢复到原始位置。
- 可为新增单集自动截取默认 JPG。

### 完整性检查与修复

- 检查缺失 `tvshow.nfo`、`poster.jpg`、`fanart.jpg`。
- 检查缺失或无法解析的 Episode NFO。
- 检查缺失 Episode JPG。
- 检查 Episode 编号断档与重复编号。
- 支持补齐缺失 `tvshow.nfo`、Episode NFO，并可批量为缺失 Episode JPG 自动截取中间帧。

### 稳定性

- 继续沿用 v0.1.3 已实机验证的绿联兼容输出规则：家庭影像 UI 不暴露 Season，但磁盘结构保持 `Season 01`。
- 为覆盖已有 NFO 的任务新增可撤销 `replace` 操作，并在撤销前校验文件是否再次被修改。
- 集合列表扫描避免在列表页对全部视频执行 ffprobe，降低大媒体库浏览成本。

### 验证状态

- 自动化测试：15 项通过。
- Web 路由验证：首页、媒体浏览、集合列表、集合详情、增量选择与增量预览均返回 200。
- 增量执行 → 任务记录 → 撤销往返流程验证通过。

## v0.1.3 - 2026-08-17

### 绿联兼容输出

- 家庭影像模式在界面中保持“无季”概念，但底层默认使用 `Season 01` 目录以提高绿联影视中心的剧集归组稳定性。
- 家庭影像模式下 Episode 文件名收敛为 `集合名 - S01E01`，避免随机 Hash 或标题干扰扫描器判断。
- Episode NFO 保留 `<season>1</season>` 与 `<episode>N</episode>`。
- 家庭影像兼容模式不生成 `season.nfo`。
- 纯 Hash 原始文件名默认转换为 `第1集 / 第2集 / ...` 的可读标题建议。
- 支持自动补齐未手工选择的单集封面：默认从视频约 50% 位置截取画面。
- 增加绿联扫描验证提示。

### 已有能力

- NAS 目录浏览与视频扫描。
- 视频时长、分辨率、编码、大小、修改时间读取。
- 当前目录视频全选 / 取消全选 / 已选数量。
- 视频集合编排与 Episode 自动编号。
- 集合和单集元数据编辑。
- `poster.jpg` / `fanart.jpg` 支持上传或从集合内指定视频截取候选画面。
- 单集支持 FFmpeg 候选截图。
- 支持“整理并生成媒资”与“仅生成媒资”。
- 执行前冲突检测、预览、默认不覆盖已有文件。
- SQLite 保存草稿、任务和撤销信息。
- 支持执行失败回滚与成功任务撤销。

### 验证状态

- 自动化测试：10 项通过。
- Docker + FastAPI 启动链路验证通过。
- 绿联 NAS 影视中心实机验证：同一家庭影像集合可被正确归组识别。
