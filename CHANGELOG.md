# Changelog

本项目遵循语义化版本号的思路进行版本归档。当前仓库从已在绿联 NAS 实机验证的 `v0.1.3` 开始归档。

## v0.1.6 - 2026-08-17

### 手机端体验

- 新增独立 `mobile.css` 响应式层，不改动已经稳定的媒体整理核心逻辑。
- 以 360～430px Android 竖屏作为主要适配范围，同时保留桌面端宽屏布局。
- 手机端新增固定底部导航：媒体 / 集合 / 任务。
- 页面加入 `viewport-fit=cover` 和底部安全区域适配，避免全面屏手势区域遮挡按钮。
- 触控按钮、checkbox / radio、表单输入区域统一增大；输入框在移动端使用 16px 字号，避免浏览器聚焦自动缩放。
- 两列 / 三列表单在窄屏自动切为单列。
- Episode 文件名、路径和预览长文本支持换行，不再依赖横向滚动。

### 媒体选择

- 媒体文件页在手机上从宽表格转换为卡片式列表。
- 视频和可批量处理文件夹支持点击整行切换勾选状态。
- 文件选择工具栏固定在底部导航上方，长目录滚动时仍可查看已选视频 / 文件夹数量并继续操作。
- 文件卡片保留时长、分辨率、大小和修改时间等关键辅助信息，弱化编码等次要字段。
- 增量添加视频页面采用相同的手机端文件选择交互，并移除重复的“取消全选”按钮。

### 图片与编排

- 单集截图、poster 和 fanart 候选图在手机端改为横向滑动选择，不再压缩为狭窄的多列网格。
- 集合信息、Episode 编排和维护表单统一使用更适合单手操作的触控高度。
- 底部“保存 / 预览 / 执行”等关键操作继续保持 sticky，并避开手机底部主导航。

### 预览与集合维护

- 文件变更预览在手机端由左右“原路径 → 新路径”改为上下排列。
- 批量集合预览在手机端转换为纵向信息卡片，长目标路径可自然换行。
- 集合列表、完整性检查、快速修复和 Episode 维护针对窄屏进一步收敛布局。

## v0.1.5 - 2026-08-17

### 文件选择体验

- 视频选择页移除重复的“取消全选”按钮，保留一个复选框即可完成全选 / 取消全选。
- “全选当前目录视频”文案收敛为“全选视频”。
- 文件夹现在可独立勾选，用于批量创建视频集合；视频选择和文件夹选择互不干扰。
- 文件夹行会显示当前层可处理的视频数量；空目录、无视频目录和已整理集合不会提供批量勾选入口。

### 默认时间

- 新建集合的年份默认使用程序运行时的当前年份，不再留空让媒体中心回退到 `1970`。
- Episode 默认日期优先使用原视频文件修改日期。
- 如果文件时间落在异常的 Unix epoch 区间（例如 1970）或明显超出合理范围，则自动回退为当天日期。

### 批量文件夹生成集合

- 支持一次勾选多个文件夹，每个文件夹自动生成一个独立集合。
- 集合名称默认直接使用文件夹名称。
- 文件夹内视频按修改时间和文件名稳定排序后自动编号为 E01、E02、E03……。
- 批量模式只处理所选文件夹当前层的视频，不递归合并子目录，避免误把多个子集合混在一起。
- 自动过滤空目录、无支持视频的目录、已有 `tvshow.nfo` 的集合，以及疑似已存在 `Season xx + SxxExx` 结构的目录。
- 执行前提供批量预览，明确展示可生成集合、被过滤目录和冲突项。
- 输出继续采用已在绿联影视中心验证的 `Season 01 + SxxExx` 兼容结构。
- 默认自动生成 Episode JPG；集合级 poster / fanart 仍可后续通过集合维护能力补充或替换。
- 一个批量任务中的多个集合统一记录到任务历史；中途失败时会尝试回滚此前已经完成的集合，成功任务仍可整体撤销。

### Docker 部署与自动发布

- 默认 `docker-compose.yml` 从源码 `build` 改为直接使用 `ghcr.io/rosenray/nas-media-manager:latest`。
- 新增 `docker-compose.local.yml`，开发调试时仍可从源码本地构建。
- GitHub Actions 在 `main` 测试通过后自动构建并发布 Docker 镜像。
- 同时构建 `linux/amd64` 与 `linux/arm64`，兼容常见 x86 与 ARM NAS。
- 每次发布同时推送 `latest` 和 `VERSION` 文件对应的版本标签。
- NAS 后续更新只需要重新拉取镜像并重建容器，不需要重新上传项目源码。
- 更新 GitHub Actions 到当前 Node 24 兼容的 Action 主版本。

### 验证

- 新增 5 项批量流程与默认日期测试，覆盖 1970 回退、目录过滤、文件夹名称映射、浅层扫描以及批量执行 / 撤销。
- GitHub Actions 自动执行完整 pytest 回归；Docker 镜像发布依赖测试成功。

## v0.1.4 - 2026-08-17

### 集合维护

- 新增“已整理集合”入口，自动发现带 `tvshow.nfo` 或绿联兼容 `Season 01 + SxxExx` 结构的集合。
- 集合详情页可查看集合信息、Episode 列表、海报/背景存在状态与完整性检查结果。
- 支持修改集合标题、简介、年份、Genre；重写 `tvshow.nfo` 时同步已有 Episode NFO 的 `showtitle`。
- 支持修改单集标题、简介和日期；只更新对应 NFO，不主动重命名视频文件。
- 集合级和单集级 NFO 更新均写入任务记录，可撤销恢复修改前内容。

### 增量追加

- 可向已有集合继续添加新视频。
- 自动读取 Season 01 当前最大 Episode 编号，从下一集连续编号。
- 追加前可编辑新增单集标题、简介和日期。
- 追加前提供文件移动与媒资生成预览及冲突检测。
- 只处理新增视频，不修改已有 Episode。
- 增量任务支持撤销，视频可恢复到原始位置。
- 可为新增单集自动截取默认 JPG。

### 完整性检查与修复

- 检查缺失 `tvshow.nfo`、`poster.jpg`、`fanart.jpg`。
- 检查缺失或无法解析的 Episode NFO。
- 检查缺失 Episode JPG。
- 检查 Episode 编号断档与重复编号。
- 支持补齐缺失 `tvshow.nfo`、Episode NFO，并可批量为缺失 Episode JPG 自动截取中间帧。

### 稳定性

- 继续沿用 v0.1.3 已实机验证的绿联兼容输出规则：家庭影像 UI 不暴露 Season，但磁盘结构保持 `Season 01`。
- 为覆盖已有 NFO 的任务新增可撤销 `replace` 操作，并在撤销前校验文件是否再次被修改。
- 集合列表扫描避免在列表页对全部视频执行 ffprobe，降低大媒体库浏览成本。

### 验证状态

- 自动化测试：15 项通过。
- Web 路由验证：首页、媒体浏览、集合列表、集合详情、增量选择与增量预览均返回 200。
- 增量执行 → 任务记录 → 撤销往返流程验证通过。

## v0.1.3 - 2026-08-17

### 绿联兼容输出

- 家庭影像模式在界面中保持“无季”概念，但底层默认使用 `Season 01` 目录以提高绿联影视中心的剧集归组稳定性。
- 家庭影像模式下 Episode 文件名收敛为 `集合名 - S01E01`，避免随机 Hash 或标题干扰扫描器判断。
- Episode NFO 保留 `<season>1</season>` 与 `<episode>N</episode>`。
- 家庭影像兼容模式不生成 `season.nfo`。
- 纯 Hash 原始文件名默认转换为 `第1集 / 第2集 / ...` 的可读标题建议。
- 支持自动补齐未手工选择的单集封面：默认从视频约 50% 位置截取画面。
- 增加绿联扫描验证提示。

### 已有能力

- NAS 目录浏览与视频扫描。
- 视频时长、分辨率、编码、大小、修改时间读取。
- 当前目录视频全选 / 取消全选 / 已选数量。
- 视频集合编排与 Episode 自动编号。
- 集合和单集元数据编辑。
- `poster.jpg` / `fanart.jpg` 支持上传或从集合内指定视频截取候选画面。
- 单集支持 FFmpeg 候选截图。
- 支持“整理并生成媒资”与“仅生成媒资”。
- 执行前冲突检测、预览、默认不覆盖已有文件。
- SQLite 保存草稿、任务和撤销信息。
- 支持执行失败回滚与成功任务撤销。

### 验证状态

- 自动化测试：10 项通过。
- Docker + FastAPI 启动链路验证通过。
- 绿联 NAS 影视中心实机验证：同一家庭影像集合可被正确归组识别。
